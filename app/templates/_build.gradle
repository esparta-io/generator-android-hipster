buildscript {

  repositories {
    mavenCentral()
    jcenter()
  }
  dependencies {
    // android-hipster-needle-gradle-parent-dependency
  }
}

allprojects {
  repositories {
    mavenCentral()
    jcenter()
  }
}

task clean(type: Delete) {
  delete rootProject.buildDir
}
