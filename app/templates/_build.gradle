buildscript {

  repositories {
    mavenCentral()
    jcenter()
  }
  dependencies {

    // android-hipster-needle-gradle-dependency
  }
}

allprojects {
  repositories {
    mavenCentral()
    jcenter()
  }
}

task clean(type: Delete) {
  delete rootProject.buildDir
}
